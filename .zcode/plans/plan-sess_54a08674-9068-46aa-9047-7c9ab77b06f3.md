## Admin Navigation UI/UX — Visual-Only Corrections (no function changes)

**Constraint:** Every `onClick`, state variable, data fetch, modal, and the `?tab=` routing stays byte-for-byte identical. Only markup/styling/CSS changes.

### Issues being corrected

**1. Sidebar (`src/components/Sidebar.jsx`)**
- **JS-injected hover styles** (`onMouseEnter/Leave` mutating `element.style`) → replaced with proper CSS classes (`.admin-nav-item`, `:hover`, `.is-active`) in `admin-overrides.css`. Same hover colors, no flicker, keyboard `:focus-visible` rings added.
- **No visual hierarchy**: flat list under one "Navigation" label → grouped with section labels **Overview** (Dashboard), **Pipeline** (Leads Pipeline, Reports & Analytics), **Content** (Templates, Blog Posts). Same 5 buttons, same order, same `setActiveTab` calls.
- **Mobile drawer has no slide animation** (mounts already at `translateX(0)`) → add a CSS `@keyframes` slide-in on mount. Close stays instant (no JS needed).
- Minor polish: refined active state (keeps the red tint + left accent identity), consistent hamburger button styling, spacing/typography cleanup.

**2. Page titles don't match nav labels (wayfinding issue)**
Nav says "Leads Pipeline / Reports & Analytics / Templates / Blog Posts" but page headers read "Leads Pipeline Management / Franchise Reports & Funnel / Communication Templates / Blog & Articles CMS". Corrected so each page's H1 matches its nav label; the descriptive text moves into the subtitle line (existing copy preserved there).

**3. Inconsistent page headers**
- **Dashboard** (`FranchiseDashboard.jsx`) has no header at all → add a banner-card header (icon chip + "Dashboard" + subtitle). Pure addition, nothing moved or removed.
- **Leads** (`LeadsPage.jsx`) uses a bare inline-styled title, unlike the white banner cards on the other pages → restyle to the same banner-card pattern. "Add New Lead" button and handler untouched.
- **Reports / Templates / Blog**: already banner cards — only their icon chips recolor from emerald to brand red for consistency with the sidebar and primary buttons. Everything else untouched.
- Header action buttons ("Add New Lead", "New Template", "New Post", "Export Leads (CSV)") keep their exact handlers; only classes/spacing harmonized.

### Files changed
- `src/components/Sidebar.jsx` — markup/class changes only
- `src/components/FranchiseDashboard.jsx` — header block added at top
- `src/components/LeadsPage.jsx` — header restyle
- `src/components/ReportsPage.jsx`, `TemplatesPage.jsx`, `BlogPostsPage.jsx` — title text + icon chip color only
- `src/admin-overrides.css` — new admin nav classes, focus rings, drawer keyframes

### Explicitly NOT doing (would change functions)
No collapse toggle, no live count badges, no sidebar/top-bar quick-action menus, no URL-param action triggers, no moving of the notification polling, no CSV extraction.

### Verification
1. `npm run build` — clean compile.
2. Dev server + browser screenshots: desktop sidebar (grouped states, active/hover/focus), mobile drawer slide-in, all 5 page headers, and confirm no style bleed onto the public site.